#include<stdio.h>

void main()
{
    /* unsigned char cmin = 0, cmax = 0;
    cmax--;
    printf("Unsigned char range:\t\t%u  -  %u\t\t\n", cmin, cmax);

    signed char cmin2, cmax2;
    cmin2 = cmax2 = cmax / 2;
    cmin2*=-1;
    --cmin2;
    printf("Signed char range:\t\t%d  -  %d\t\t\n", cmin2, cmax2);


    
    unsigned short smin = 0, smax = 0;
    smax--;
    printf("Unsigned short range:\t\t%u  -  %u\t\t\n", smin, smax);

    signed short smin2, smax2;
    smin2 = smax2 = smax / 2;
    smin2*=-1;
    --smin2;
    printf("Signed short range:\t\t%d  -  %d\t\t\n", smin2, smax2);

    

    unsigned long lmin = 0, lmax = 0;
    --lmax;
    printf("Unsigned long range:\t\t%lu  -  %lu\t\t\n", lmin, lmax);

    signed long lmin2, lmax2;
    lmin2 = lmax2 = lmax / 2;
    lmin2*=-1;
    --lmin2;
    printf("Signed long range:\t\t%ld  -  %ld\t\t\n", lmin2, lmax2);



    unsigned int imin = 0, imax = 0;
    --imax;
    printf("Unsigned int range:\t\t%u  -  %u\t\t\n", imin, imax);

    signed int imin2, imax2;
    imin2 = imax2 = imax / 2;
    imin2*=-1;
    --imin2;
    printf("Signed int range:\t\t%d  -  %d\t\t\n", imin2, imax2);
    */


   signed double fmin = 0, fmax = 0;
   --fmax;
   printf("Unsigned float range:\t\t%f  -  %f\t\t\n", fmin, fmax);
}